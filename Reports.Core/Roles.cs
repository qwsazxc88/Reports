namespace Reports.Core
{
    // same is id in database
    public enum UserRole
    {
        Admin = 1,
        Employee = 2, 
        Manager = 3,
        PersonnelManager = 4,
        BudgetManager = 5,
        OutsourcingManager = 6,
    }
}
